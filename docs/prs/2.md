# PR notes: Issue #2 - Vendor Jarvis and scaffold config

Plan: https://github.com/mkromberg/media-browser/blob/0c95234/docs/plans/original-plan.md#L19-L100

## Cycle 1: RED

### Test surface

`apl-service/tests/unit/ScaffoldTests.aplf` (namespace script, seven `Test_*` functions) plus a fixture, `apl-service/tests/fixtures/orphan_jarvis_config/jarvis.config.json`, and the harness that runs them, `apl-service/tests/run-tests.apls`.

The harness fixes `APLSource/*.aplf` and `tests/unit/*.aplf` with one helper, `Fix←{2 ⎕FIX'file://',⍵}`. `2 ⎕FIX` fixes "a series of named namespaces or function definitions, or a combination" from a script file, so the same call handles a bare function (`APLSource/*.aplf`) and a namespace script bundling several functions (`tests/unit/*.aplf`) - default `⎕FIX` (`X=1`) rejects a bare tradfn, which is the only reason an earlier draft split this into two helpers. `2 ⎕FIX` returns the established name(s) as strings rather than a reference, so the harness does `⍎name` to get a reference where it needs one (test namespaces), and nothing further for source files, where the function only needs to be established under its own name. The harness then discovers niladic `Test_*` functions per namespace via `⎕NL 3`, runs each under `:Trap 0`, and exits 0/1 on aggregate pass/fail.

`run-tests.apls` (not `.aplf`): it is a file dyalogscript executes directly as top-level statements and never fixes, as distinct from `.aplf`, which now names only a file `⎕FIX`ed into the workspace. The plan is updated accordingly, including for the two other script files later issues will add (`scripts/start-server.apls`, `tests/fixtures/generate_fixtures.apls`).

### Coverage table

| Acceptance criterion (issue #2) | Test |
| --- | --- |
| Vendored `Jarvis.dyalog` is present and loadable | `Test_VendoredJarvisFixes` |
| `VENDORED_FROM.md` records source URL, commit SHA, fetch date | `Test_VendoredFromRecordsProvenance` |
| `jarvis.config.json` carries native Jarvis keys only, with the plan's values | `Test_JarvisConfigHasExpectedNativeKeys` |
| `app.config.json` carries a `rootFolder` key | `Test_AppConfigHasRootFolder` |
| Directory layout matches the plan's repository layout section | `Test_DirectoryLayoutScaffolded` |
| `CodeLocation` resolves relative to the config file's own directory (real case: sibling `APLSource` exists) | `Test_CodeLocationResolvesRelativeToRealConfigFile` |
| `CodeLocation` resolution is anchored to the config file's directory, not some fixed or cwd-relative path (orphan case: no sibling `APLSource`) | `Test_CodeLocationDoesNotFollowAConfigFileMovedWithoutItsSibling` |

The last two tests instantiate the real vendored `Jarvis` class and call `.Start`, distinguishing two `CheckCodeLocation` outcomes read from Jarvis's own source (`Source/Jarvis.dyalog:624-671`): `rc=8` with a message naming the missing `AppInitFn` means the `CodeLocation` folder itself resolved correctly (a folder-not-found result, `rc∈5 6 7`, would mean it did not). Since `Initialize.aplf` does not exist until issue #7, the real config case is expected to stop at `rc=8`, not `rc=0`; that is what proves resolution succeeded without depending on `Initialize` also existing yet.

### RED confirmation

Before `apl-service/vendor/`, `apl-service/config/`, and the rest of the scaffold existed, all seven tests failed (file-not-found or `⎕FIX`/`⎕NEW` errors), for exit code 1:

```
FAIL #.ScaffoldTests.Test_AppConfigHasRootFolder
FAIL #.ScaffoldTests.Test_CodeLocationDoesNotFollowAConfigFileMovedWithoutItsSibling
FAIL #.ScaffoldTests.Test_CodeLocationResolvesRelativeToRealConfigFile
FAIL #.ScaffoldTests.Test_DirectoryLayoutScaffolded
FAIL #.ScaffoldTests.Test_JarvisConfigHasExpectedNativeKeys
FAIL #.ScaffoldTests.Test_VendoredFromRecordsProvenance
FAIL #.ScaffoldTests.Test_VendoredJarvisFixes
0/7 passed
```

### Anticipated implementation order

1. Vendor `Source/Jarvis.dyalog` from `github.com/Dyalog/Jarvis` verbatim, plus `VENDORED_FROM.md`.
2. `config/jarvis.config.json` and `config/app.config.json`.
3. Scaffold the remaining empty directories (`APLSource`, `frontend`, `scripts`, `tests/e2e`) with `.gitkeep` placeholders.
